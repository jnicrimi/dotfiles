keybinds {
    shared {
        bind "Ctrl b" { Clear; }
        unbind "Ctrl o"
        unbind "Ctrl q"
        unbind "Ctrl r"
    }
}

ui {
    pane_frames {
        hide_session_name true
    }
}
